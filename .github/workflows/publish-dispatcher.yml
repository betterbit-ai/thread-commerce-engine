name: Legacy publish dispatcher disabled
on: { workflow_dispatch: {} }
permissions: { contents: read }
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "::notice::Legacy long-form publishing is disabled. Use Publish scheduled equipment threads."
